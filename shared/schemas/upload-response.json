{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UploadResponse",
  "description": "Schema for POST /v1/upload success response (200)",
  "type": "object",
  "required": ["request_id", "message_id", "version", "uploaded_at"],
  "properties": {
    "request_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for this upload request"
    },
    "message_id": {
      "type": "string",
      "description": "Telegram message ID for the uploaded file"
    },
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "Version number assigned to this upload"
    },
    "uploaded_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when the upload completed"
    }
  },
  "additionalProperties": false
}
